{
  "name": "@illuma/react-experimental-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable example for @illuma/react-experimental: root container, screen scope, mount-owned resources, signals and a dependency swapped in a test.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
